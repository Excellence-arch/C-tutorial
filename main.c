#include <stdio.h>
#include <stdlib.h>
#define MAX_LIMIT 20
int main() {
    //char a[MAX_LIMIT];
    //scanf("%c", &a);
    //gets(a);
    //fgets(a, MAX_LIMIT, stdin);
    //scanf("%[^\n]%c", a);
    scanf("%[^\n]s", a);
    printf("%s", a);
    return 0;
}
